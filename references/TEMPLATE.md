# <Pattern name>

Platforms: <Web | Mobile | Site | Desktop — list every platform this pattern applies to>
Tier: <Fundamental | Common | Conditional — must match the same pattern's tier in every section index it appears in; see SKILL.md's Judgment calls for what each means. Add one clause on what would make it not apply, e.g. "assume it applies unless X".>

<One or two sentences defining what this screen/feature is and when a product has one.>

- [ ] **<Item name>** — <what it is and why it matters. Where a detail is platform-specific, say so inline, e.g. "(mobile only)" or "(web: use the `autocomplete` attribute; mobile: use the platform's autofill API)".>
- [ ] **<Item name>** — <...>

## Notes

<Optional: anything that doesn't fit as a checklist item — a citation to a platform guideline, a regulatory requirement, a common mistake worth flagging. Omit this section if there's nothing to add.>
